# @langchain/community pulls peers (e.g. @browserbasehq/stagehand) that still
# declare zod@^3 while this app uses zod@4. npm's strict peer resolution blocks
# installs; legacy mode matches what many LangChain + Zod 4 projects use.
legacy-peer-deps=true
